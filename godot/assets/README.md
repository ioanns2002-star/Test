# Generated scene assets

The package step places these untracked files here:

- `scene.glb` — geometry and embedded/exported material data from Blender.
- `scene-manifest.json` — reference camera values consumed at runtime.

`main.gd` deliberately handles their absence with an on-screen diagnostic, so this
starter project can still open before packaging.
